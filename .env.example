# Zkopíruj tento soubor jako .env a vyplň hodnoty
# cp .env.example .env

# Povinné — heslo pro PostgreSQL
POSTGRES_PASSWORD=zmen_toto_na_silne_heslo

# Volitelné — port na kterém poběží aplikace (výchozí 8080)
APP_PORT=8080
